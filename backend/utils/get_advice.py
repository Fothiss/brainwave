import os
from qdrant_client import QdrantClient
from langchain_gigachat.chat_models.gigachat import GigaChat
from langchain_gigachat.embeddings import GigaChatEmbeddings

# Настройки подключения
QDRANT_HOST = os.getenv("QDRANT_HOST")
QDRANT_PORT = os.getenv("QDRANT_HTTP_PORT")
COLLECTION_NAME = "legal_rules_chunks"

# Инициализация клиентов
qdrant_client = QdrantClient(host=QDRANT_HOST, port=QDRANT_PORT)

# Инициализация модели GigaChat
model = GigaChat(
    model="GigaChat-2",
    temperature=0.1,
    max_tokens=1000,
    credentials=os.getenv("AUTH_KEY"),
    verify_ssl_certs=False
)

# Получаем эмбеддинг для запроса    
embeddings = GigaChatEmbeddings(
    model="EmbeddingsGigaR",
    credentials=os.getenv("AUTH_KEY"),
    verify_ssl_certs=False
)


def search_relevant_chunks(query: str, section_number: str = None, limit: int = 5):
    """
    Поиск релевантных чанков в векторной БД
    
    Args:
        query: Поисковый запрос
        section_number: Номер раздела для фильтрации (опционально)
        limit: Количество возвращаемых результатов
    
    Returns:
        List[dict]: Список релевантных чанков
    """
    # Создаем поисковый запрос
    search_query = query

    # Если указан номер раздела, добавляем его в запрос для повышения релевантности
    if section_number:
        search_query = f"{section_number} {query}"

    query_embedding = embeddings.embed_query(search_query)

    # Выполняем поиск в Qdrant
    search_results = qdrant_client.query_points(
        collection_name=COLLECTION_NAME,
        query=query_embedding,
        limit=limit
    )

    # Форматируем результаты
    relevant_chunks = []
    for result in search_results.points:
        relevant_chunks.append({
            "text": result.payload.get("text", ""),
            "score": result.score
        })

    return relevant_chunks


def create_prompt(operation: str, participant_type: str, section_number: str, main_chunks: list, deadline_chunks: list) -> str:
    """
    Создание промпта для LLM на основе контекста и пользовательского запроса
    
    Args:
        operation: Название операции
        participant_type: Тип лица
        section_number: Номер раздела
        main_chunks: Список релевантных чанков для основного контекста
        deadline_chunks: Список релевантных чанков для контекста о сроках
    
    Returns:
        str: Сформированный промпт
    """
    # Формируем основной контекст из релевантных чанков
    main_context = "\n\n".join([f"Контекст {i + 1} (релевантность: {chunk['score']:.3f}):\n{chunk['text']}"
                    for i, chunk in enumerate(main_chunks)])

    # Формируем контекст о сроках из пункта 4.4
    deadline_context = "\n\n".join([f"Контекст {i + 1} (релевантность: {chunk['score']:.3f}):\n{chunk['text']}"
                for i, chunk in enumerate(deadline_chunks)])

    # Формируем основной промпт
    prompt = f"""
        Ты - юридический ассистент, который помогает разобраться в правилах и процедурах.
        На основе предоставленного контекста ответь на вопрос пользователя.

        ИНФОРМАЦИЯ О ЗАПРОСЕ:
        - Операция: {operation}
        - Тип лица: {participant_type}
        - Раздел правил: {section_number}

        ОСНОВНОЙ КОНТЕКСТ ИЗ РАЗДЕЛА {section_number}:
        {main_context}

        УНИВЕРСАЛЬНЫЙ КОНТЕКСТ О СРОКАХ ИЗ РАЗДЕЛА 4.4:
        {deadline_context}

        ИНСТРУКЦИИ ДЛЯ ФОРМИРОВАНИЯ ОТВЕТА:

        1. АНАЛИЗ И ПРИНЦИПЫ:
        - Внимательно изучи ОСНОВНОЙ контекст из раздела {section_number}.
        - Если в ОСНОВНОМ контексте ЕСТЬ конкретные сроки выполнения операции ({operation}) - используй ТОЛЬКО их.
        - Если в ОСНОВНОМ контексте НЕТ конкретных сроков - используй УНИВЕРСАЛЬНЫЕ сроки из раздела 4.4.
        - Для пунктов "Документы" и "Действия" используй ТОЛЬКО ОСНОВНОЙ контекст раздела {section_number}.

        2. СТРУКТУРА ОТВЕТА (соблюдай точно эту структуру):

        Операция: {operation}
        Тип лица: {participant_type}
        Раздел правил: {section_number}

        1. НЕОБХОДИМЫЕ ДОКУМЕНТЫ:
        • [Перечисли все документы, заявления и анкеты, указанные в основном контексте. 
        Для анкет укажи, какие документы нужны для их заполнения. Если информации нет - так и укажи]

        2. СРОКИ ВЫПОЛНЕНИЯ ОПЕРАЦИИ:
        • [Укажи срок, найденный по правилам из п.1 анализа. 
        Если срок из основного раздела, НЕ упоминайте раздел 4.4. 
        Если срок из раздела 4.4, укажите это.]
        
        3. ДЕЙСТВИЯ В СИСТЕМЕ ВЕДЕНИЯ РЕЕСТРА:
        • [Подробно опиши шаги, процедуры, порядок внесения записей, проверки и условия, используя только основной контекст.]

        ЖЕСТКИЕ ОГРАНИЧЕНИЯ И ТРЕБОВАНИЯ:
        - НИКОГДА не объединяй информацию о сроках из двух разделов в одном ответе. Выбери ОДИН источник для срока.
        - НИКОГДА не пиши в ответе вступительные фразы, заключения, подведения итогов.
        - НИКОГДА не используй слова "контекст", "релевантность", "коэффициент", "согласно пункту", "на основании".
        - НИКОГДА не показывай свои внутренние размышления, расчеты или сомнения. Пользователь должен видеть только готовый ответ.
        - Ответ должен быть самодостаточным и готовым к использованию.

        ПРИМЕР ОТВЕТА:
            Операция: Открытие лицевого счета
            Тип лица: Физическое лицо
            Раздел правил: 5.1.

            ### 1. НЕОБХОДИМЫЕ ДОКУМЕНТЫ:
            • Заявление об открытии лицевого счета
            • Заполненная анкета физического лица
            Анкету подписывает само физическое лицо либо его законный представитель.
            Документы, подтверждающие достоверность сведений, указанных в анкете или заявлении, необходимы, если эти сведения не подтверждены с использованием информации из государственных информационных систем.

            ### 2. СРОКИ ВЫПОЛНЕНИЯ ОПЕРАЦИИ:
            Регистратор обязан открыть лицевой счет или отказать в его открытии в течение пяти рабочих дней со дня получения заявления и заполненной анкеты.

            ### 3. ДЕЙСТВИЯ В СИСТЕМЕ ВЕДЕНИЯ РЕЕСТРА:
            1. Регистратор принимает заявление и заполненную анкету физического лица.
            2. Проверяет правильность оформления документов и достоверность указанных сведений.
            3. Вносит запись о лицевом счете в учетные регистры системы ведения реестра.
            4. Указывает реквизиты нового лицевого счета в регистрационном журнале.
            5. Сообщает заявителю решение об открытии лицевого счета или отказе в письменной форме.

        """
    return prompt


def get_llm_response(prompt: str) -> str:
    """
    Получение ответа от LLM GigaChat
    
    Args:
        prompt: Сформированный промпт
    
    Returns:
        str: Ответ от модели
    """
    try:
        response = model.invoke(prompt)
        return response.content
    except Exception as e:
        return f"Ошибка при обращении к LLM: {str(e)}"


def get_legal_advice(operation: str, participant_type: str, section_number: str) -> str:
    """
    Основная функция для получения юридической консультации
    
    Args:
        operation: Название операции (например, "Открытие лицевого счета")
        participant_type: Тип лица (например, "Физическое лицо")
        section_number: Номер раздела правил (например, "5.1")
    
    Returns:
        str: Понятный ответ пользователю
    """
    print(f"🔍 Поиск информации: операция='{operation}', лицо='{participant_type}', раздел='{section_number}'")

    # 1. Формируем основной запрос - информация об операции
    main_search_query = f"{operation} {participant_type}"
    main_chunks = search_relevant_chunks(
        query=main_search_query,
        section_number=section_number,
        limit=7
    )

    if not main_chunks:
        return "К сожалению, не удалось найти релевантную информацию в базе правил. Пожалуйста, уточните параметры запроса."

    print(f"📚 Найдено {len(main_chunks)} основных релевантных фрагментов")
    print("⏰ Поиск информации о сроках")

    # 2. Формируем дополнительный запрос - информация о сроках
    deadline_search_query = "Сроки выполенения операций, время, период дней"
    deadline_chunks = search_relevant_chunks(
        query=deadline_search_query,
        section_number="4.4",
        limit=4
    )

    print(f"📚 Найдено {len(deadline_chunks)} релевантных фрагментов о сроках")


    # 3. Создаем промпт
    prompt = create_prompt(operation, participant_type, section_number, main_chunks, deadline_chunks)

    # 4. Получаем ответ от LLM
    response = get_llm_response(prompt)

    return response


# Пример использования
if __name__ == "__main__":
    # Тестовый пример
    result = get_legal_advice(
        operation="Переход прав собственности на ЦБ",
        participant_type="Физическое лицо",
        section_number="5.3."
    )

    print("\n" + "=" * 50)
    print("РЕЗУЛЬТАТ:")
    print("=" * 50)
    print(result)
