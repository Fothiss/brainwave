<h1 align="center">🧠 BrainWave</h1>
<p align="center"> Интеллектуальный сервис, объединяющий законы, регламенты и пользовательскую документацию, чтобы давать <b>быстрые, точные и понятные ответы</b> на сложные запросы. </p>
<p align="center"> ⚡ Экономит время&nbsp;&nbsp;·&nbsp;&nbsp;🎯 Повышает точность&nbsp;&nbsp;·&nbsp;&nbsp;⭐ Улучшается благодаря оценкам сотрудников </p>

<h2>🚀 Описание проекта</h2>
<p> <b>BrainWave</b> — это единая точка доступа к знаниям компании. Он объединяет данные из разных источников: </p> <ul> <li>📘 государственные законы</li> <li>🔐 правила проведения операций</li> <li>🖥️ руководство пользователя</li> </ul> 
<p> ИИ-помощник анализирует эти источники и формирует <b>единый и точный ответ</b>. Сотрудники могут оценивать качество ответа, что позволяет улучшать алгоритмы BrainWave. </p>

<h2>🗄️ Технологический стек</h2>
<table> 
<tr> <td>⚛️ <b>Next.js + Assistant UI</b></td><td>Фронтенд, интерфейс чата, поток сообщений, интеграция с ИИ</td> </tr> 
<tr> <td>🐍 <b>Django (Backend API)</b></td> <td>Обработка запросов, маршруты для агентов, взаимодействие с БД и Qdrant</td> </tr> 
<tr> <td>🐘 <b>PostgreSQL</b></td> <td>Хранение структурированных данных, логов, оценок, истории взаимодействий</td> </tr> 
<tr> <td>🧩 <b>Qdrant</b></td> <td>Векторное хранилище эмбеддингов, быстрый поиск похожих документов</td> </tr> <tr> <td>🐳 <b>Docker Compose</b></td> <td>Оркестрация всех сервисов: фронтенд, бэкэнд, БД и Qdrant</td> </tr> 
</table>

<h2>⚙️ Переменные окружения</h2>
<p>Создайте файл <code>.env</code> в корне проекта и добавьте параметры:</p>

```text
# PostgreSQL
DB_PORT=5432
DB_NAME=name
DB_USER=user
DB_PASSWORD=password
DB_HOST=postgres (название из docker-compose.yml)

# Qdrant
QDRANT_HTTP_PORT=6333
QDRANT_GRPC_PORT=6334
QDRANT_HOST=qdrant (название из docker-compose.yml)

# Backend
BACKEND_PORT=8000
SECRET_KEY=*********
DEBUG=True
ALLOWED_HOSTS=*

# Frontend
FRONTEND_PORT=3000
NEXT_PUBLIC_BACKEND_URL=http://brainwave_backend.com

# GigaChat
CLIENT_ID=xxx
CLIENT_SECRET=xxx
AUTH_KEY=xxx

# Telegram
TG_TOKEN=xxx
TG_CHAT_ID=xxx
```

<h2>🔑 Google Drive Service Account</h2>
<p>
    В директории <code>backend/utils</code> необходимо разместить файл 
    <code>service_account.json</code>, полученный в 
    <strong>Google Cloud Console</strong> 
    (раздел <em>Service Accounts → Keys</em>).
</p>
<p>
    Этот файл необходим для корректной работы с <strong>Google Drive API</strong>.
</p>

<h2>▶️ Запуск проекта</h2>
<p>Проект запускается с помощью <code>docker compose</code> и включает 4 сервиса:</p> 
<ul> 
<li>⚛️ <b>frontend</b> — Next.js + Assistant UI</li> 
<li>🐍 <b>backend</b> — Django API</li> 
<li>🐘 <b>postgres_db</b> — PostgreSQL</li> 
<li>🧩 <b>qdrant</b> — векторное хранилище</li> 
</ul>

<h3>🚀 Старт сервисов</h3>

```shell
docker compose up --build -d
```

<p> После запуска сервисы будут доступны по адресам (порты из <code>.env</code>):<br> • Фронтенд →<code>http://localhost:3000</code><br> • Бэкенд →<code>http://localhost:8000</code><br> • PostgreSQL → порт <code>5432</code> <br> • Qdrant UI → <code>http://localhost:6333</code> </p>

<h3>🔄 Остановка сервисов</h3>

```shell
docker compose down
```

<h2>⭐ Обратная связь</h2> <p> BrainWave развивается вместе с пользователями. Если есть идеи или предложения —
обязательно делитесь! </p>
